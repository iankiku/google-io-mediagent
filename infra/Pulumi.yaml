name: mediagent
runtime:
  name: nodejs
  options:
    typescript: true
description: MediAgent health assistant infrastructure on Google Cloud
config:
  gcp:project:
    description: Google Cloud project ID
  gcp:region:
    default: us-central1
    description: Google Cloud region
